# このファイルを .env.local にコピーして値を設定してください。
# .env.local は .gitignore で除外され、コミットされません（機密情報用）。
# DBパス(DATABASE_URL)は非機密のため .env に置いています（コミット対象）。

# 各プロバイダーのAPIキー
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

# 同時実行数の上書き（省略時は3）
MAX_CONCURRENCY=3
